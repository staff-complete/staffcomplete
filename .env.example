# Registry — use a GitHub PAT with packages:read/write scope (or GITHUB_TOKEN in CI)
KAMAL_REGISTRY_PASSWORD=

# PostgreSQL accessory password (Kamal sets POSTGRES_PASSWORD on the db container)
POSTGRES_PASSWORD=

# Full connection string — db runs on host.docker.internal:5432 inside the app container
DATABASE_URL=postgresql://staffcomplete:POSTGRES_PASSWORD@host.docker.internal:5432/staffcomplete

# Better Auth secret — generate with: openssl rand -hex 32
AUTH_SECRET=

# Resend API key for transactional email
RESEND_API_KEY=
